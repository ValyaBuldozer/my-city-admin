import * as React from 'react';
import Answer from '../models/Answer';

interface Props {
    title: string;
    answers: Answer[];
    onUpdate: (title: string, answers: Answer[]) => any;
}

const QuzEditor = ({}: Props) => {
    return
}
